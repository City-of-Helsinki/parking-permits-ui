mutation Mutation($registration: String!) {
  getVehicleInformation(registration: $registration) {
    id
    powerType
    model
    emission
    isLowEmission
    manufacturer
    productionYear
    registrationNumber
  }
}
