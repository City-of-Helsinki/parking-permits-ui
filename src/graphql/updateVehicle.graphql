mutation Mutation($vehicleId: ID!, $registration: String!) {
  updateVehicle(vehicleId: $vehicleId, registration: $registration) {
    success
    errors
    vehicle {
      id
      powerType
      model
      emission
      isLowEmission
      manufacturer
      productionYear
      registrationNumber
    }
  }
}
