query MyProfileQuery {
  myProfile {
    id
    firstName
    lastName
    language
    addresses {
      edges {
        node {
          primary
          id
          address
          postalCode
          city
          countryCode
          addressType
        }
      }
    }
  }
}
